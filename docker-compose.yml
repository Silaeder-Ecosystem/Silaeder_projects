version: '3.9'
services:
  postgres:
    restart: always
    image: postgres:10
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=loHAXf0HTg8kamLvi1T03A-lW
      - POSTGRES_DB=silaeder_projects
    volumes:
      - ./postgres-data/postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  app:
    restart: always
    build: .
    ports:
      - 5000:5000
    volumes:
      - .:/app